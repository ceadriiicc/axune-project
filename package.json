{
  "name": "axune",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace @axune/mobile",
    "mobile:web": "npm run web --workspace @axune/mobile",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
