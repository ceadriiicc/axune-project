{
  "name": "axune",
  "version": "0.1.0",
  "private": true,
  "comments": {
    "install": "apps/mobile installs its own dependency tree on purpose. npm workspace hoisting splits Expo's tree (expo-router nested, babel-preset-expo hoisted) and breaks bundling. Shared packages are linked with file: dependencies instead."
  },
  "scripts": {
    "mobile": "npm --prefix apps/mobile run start",
    "mobile:web": "npm --prefix apps/mobile run web",
    "typecheck": "npm --prefix packages/protocol run typecheck && npm --prefix packages/shared run typecheck && npm --prefix packages/secure-channel run typecheck && npm --prefix packages/git-core run typecheck && npm --prefix packages/agent-core run typecheck && npm --prefix packages/relay run typecheck && npm --prefix apps/desktop run typecheck && npm --prefix apps/mobile run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.3.3"
  }
}
