# Axune

Shared context for both agents working on this repository lives in **[AGENTS.md](AGENTS.md)** —
read it first. It carries what Axune is, the constraints that are not negotiable, how Claude
Code and Codex divide work and record findings, and how to verify a change.

Kept as a pointer rather than a copy so the two agents cannot drift apart on the same facts.

Session-level context that is not in the repository — decisions with their reasoning, the
resume point, and the security inventory — lives in the Obsidian vault at
`Cedrics brain/01 Projects/Axune/`. Read `11 REMAINING WORK` and `10 SECURITY AND PRIVACY` at
the start of a session.
